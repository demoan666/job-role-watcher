# Handoff: Multi-provider LLM settings, usage tracking, GitHub Pages deploy issue

**Date:** 2026-08-06
**Working directory:** `H:\My Drive\Dev\Dev_jobSearch` (Google Drive-synced — known `.git` corruption/file-loss risk, accepted deliberately, see `CLAUDE.md`)
**Repo:** https://github.com/demoan666/job-role-watcher (public)
**Live site:** https://demoan666.github.io/job-role-watcher/ (currently stale — see "Unresolved" below)

## What this session did

1. **Tag summary panel + manual tags field** for the resume profile (Setup > My Profile). Committed as `070b6ad` (pushed). New `profile.manual_tags_json` DB column, structurally isolated from LLM-extracted fields so it survives resume re-extraction; manual tags union into `data/search_profile.json`'s exported `keywords`. New read-only "tag summary" panel groups Skills/Industries/Keywords/Manual with a "drives matching" vs "reference only" badge per group. Also wired `GET /profile` on page load (was previously dead code — Setup tab used to start blank every session).

2. **UI polish fixes**: removed the resume textarea's resize handle; made Setup-tab status messages turn visibly red on failure (previously blended in as regular grey text, which is why "Extract profile does nothing" read as silent failure when it was actually a 401).

3. **Multi-provider LLM settings UI + header restructure**. Committed as `4e4bbad` (pushed) — bundles both this and item 2 above (nothing was committed between them). Key pieces:
   - Header: glance notification pills moved from a full-width block into the same row as the page title, plus a new gear icon.
   - New "LLM Settings" modal (gear icon): add an API key per provider (Anthropic/OpenAI/Google — masked on display, never round-tripped in full), and independently pick which provider+model handles each of the three LLM-driven tasks (`extract_resume_profile`, `triage_message`, `draft_cold_email`).
   - Backend: `backend/config.py` gained `get_llm_settings`/`save_llm_settings`/`mask_key`, writing to a new `config.json` `"llm"` block while falling back to the legacy flat `anthropic_api_key`/`anthropic_model` fields so existing local configs kept working untouched. `backend/llm.py` refactored from Anthropic-only into a provider dispatch table (`_call_anthropic`/`_call_openai`/`_call_google`) keyed by each task's configured provider+model. New `GET`/`POST /settings/llm` endpoints in `app.py`. Added `openai` and `google-genai` to `requirements.txt` (installed in the venv).
   - Explicitly out of scope, by the user's own instruction: ChatGPT Plus/Claude Pro/Gemini Advanced *subscription logins* — no programmatic API exists for those, only real API keys work. Stated directly in the modal's copy.

4. **LLM usage tracking — built and Playwright-verified this session, NOT YET COMMITTED.** Files currently dirty: `backend/app.py`, `backend/db.py`, `backend/llm.py`, `index.html`.
   - New `llm_usage` SQLite table (`db.py`) + `log_llm_usage`/`get_llm_usage_summary` (today + all-time: calls, real input/output token counts, estimated $ cost, and a `cost_unavailable_calls` count for providers with no verified pricing).
   - `llm.py`'s three provider-dispatch functions now return real token usage from each provider's own response (`response.usage.input_tokens/output_tokens` for Anthropic, `response.usage.prompt_tokens/completion_tokens` for OpenAI, `response.usage_metadata.prompt_token_count/candidates_token_count` for Google). `_call_json` estimates cost via a `price_in`/`price_out` (USD/1M tokens) field added to each model entry in `PROVIDERS`, then logs via `db.log_llm_usage` inside a try/except that must never break the calling feature.
   - **Only Anthropic models have verified pricing** (Opus 4.8 $5/$25, Sonnet 5 $2/$10 — introductory through 2026-08-31, reverts to $3/$15 after; Haiku 4.5 $1/$5, per 1M input/output tokens). OpenAI and Google model entries have `price_in`/`price_out` set to `None` deliberately — token counts still get logged for those, cost just shows as "unknown" until real pricing is filled in. Don't fabricate numbers for those providers if asked to fill this in — get them from the provider's actual current pricing page first.
   - New `GET /usage/llm` endpoint returns the summary.
   - Frontend: a status badge in the header ("LLM usage today: $X.XXXX (N calls)", hover for full breakdown) next to the gear icon, refreshed on page load and after every extraction/lead-capture/outreach-send. A fuller "Usage" section also lives inside the LLM Settings modal (today + all-time columns).
   - Verified end-to-end via Playwright with synthetic DB rows (inserted, checked rendering, deleted afterward — no test data left in `backend/app.db`).

Also gave the user a cost-estimate chart in chat (per-task token/cost table for this app's actual prompts, per-email cost, and a monthly projection at three model tiers) — not persisted anywhere as a file, only in the conversation transcript. Re-derive from `backend/llm.py`'s system prompts if needed again rather than trusting stale numbers here.

5. **Customized the global `handoff` skill** (`C:\Users\dell\.claude\skills\handoff\SKILL.md` — applies to every project, not just this one, per explicit user request). It now:
   - Always saves into the current project's `DOCS/` directory (creating it if needed) — never the OS temp directory.
   - Before writing, checks `DOCS/` for any existing `handoff-*.md` file. If one exists, **always** asks the user (every run, no silent default) whether to overwrite it or save the new one side by side as `handoff-YYYY-MM-DD_HHMM.md`.
   - A project-local override was briefly created in this repo (`.claude/skills/handoff/SKILL.md`) before the user clarified they wanted the *global* skill changed instead — that override was deleted once the global skill matched, so this project now follows the same convention as every other project (plain `handoff-YYYY-MM-DD.md`, no `jobsearch-app` prefix — a change from this repo's own older handoff docs, which used that prefix; not reconciled, just noted).
   - **This exact file replaces two earlier attempts this session**: `DOCS/handoff-jobsearch-app-2026-08-06-1454.md` (old naming, deleted per the user's explicit "delete and replace" instruction) and an unexplained stray file `DOCS/handoff-jobsearch-app-2026-08-06-llm-settings.md` that appeared in `DOCS/` with content identical to an intermediate draft that had only ever been written to `C:\Users\dell\AppData\Local\Temp\...` and then deleted from there — the exact mechanism was never identified (not a Google Drive sync artifact, since Drive doesn't sync arbitrary Windows Temp paths into a Drive-mounted folder; more likely some path-handling quirk in this environment's file-write sandboxing). It was untracked, contained no unrecognized content, and was deleted. **Worth being alert to** if a future session sees files appearing at paths nobody explicitly wrote to.

## Current repo state

```
git status --short
 D DOCS/handoff-jobsearch-app-2026-08-06.md
 M backend/app.py
 M backend/db.py
 M backend/llm.py
 M index.html
?? DOCS/handoff-2026-08-06.md
```

The user was asked "want me to commit and push this?" for item 4 above — **no answer arrived before this handoff was written.** Don't assume consent; ask again or wait for explicit confirmation before committing/pushing item 4's changes. This handoff doc itself (and the earlier skill-customization) also haven't been offered for commit — ask before committing those too.

## Unresolved — investigate first if picking this back up

**`DOCS/handoff-jobsearch-app-2026-08-06.md` shows as locally deleted** (git status `D`), and this session never touched that file. Cause unknown — possibly the Google Drive sync file-loss risk this repo's `CLAUDE.md` already documents as an accepted risk, possibly something from an earlier session. Check `git diff DOCS/handoff-jobsearch-app-2026-08-06.md` / `git log -- DOCS/handoff-jobsearch-app-2026-08-06.md` before doing anything else that touches `DOCS/` — don't just `git checkout` it back without understanding why it's gone, per this project's own "investigate before restoring/discarding" convention.

**GitHub Pages deployment is failing, live site is stale.** The live site currently still serves commit `070b6ad`'s content (confirmed via `curl`, checking for markers only present in later commits), not `4e4bbad`. Three deployment attempts observed this session, all for `4e4bbad`:
1. First auto-triggered deploy (11:27–11:37 UTC): failed at the "Deploy to GitHub Pages" Actions step.
2. Manual re-run via GitHub Actions UI (12:29–12:39 UTC): sat stuck at `deployment_queued` for the full 10-minute watchdog window, then the job's own polling script timed out and self-canceled ("Error: Timeout reached, aborting!" / "Canceling Pages deployment...").
3. A third attempt (12:49 UTC, likely another manual re-run) failed fast (~20 seconds, queued → failure) — a different failure shape than attempt 2, not yet explained.

Checked githubstatus.com — "All Systems Operational," no open incidents for Pages/Actions, but that page only reports broad sustained outages, not a localized queue stall like this. No `gh` CLI or authenticated token was available in this environment to pull actual job logs (`GET .../actions/jobs/{id}/logs` returned 403 unauthenticated) — that's the natural next step for whoever picks this up and has an authenticated `gh` session or browser access to the Actions tab. Nothing about the repo content is implicated (failures happened in GitHub's deploy queue, before any build/upload step touched the files) — nothing here should be "fixed" in code.

**`CLAUDE.md`'s "v2: Job Search Command Center" section is now stale** relative to the actual code — it doesn't mention the manual-tags field, tag summary panel, `GET /profile` wiring, the LLM Settings modal, multi-provider `llm.py`, `/settings/llm`, or (once committed) `/usage/llm`/`llm_usage`. Worth a documentation-sync pass at some point; not urgent, just flagging the gap so nobody's surprised code and docs have diverged.

## What's verified vs not

Verified this session (Playwright browser tests against a live local backend, screenshots taken, test data/rows cleaned up after): tag summary panel + manual tags round-trip, resize/error-styling fixes, LLM Settings modal (provider key save/masking, per-task assignment save/reload, live tooltips), and the usage tracker (badge + modal detail panel, cost math cross-checked by hand for the Anthropic path).

**Not verified — no real OpenAI or Google credentials existed this session:** the `_call_openai`/`_call_google` dispatch functions were written against each SDK's documented response shape but never exercised against a live call. If the user adds a real OpenAI/Google key, the first real call through that provider is the first real test of that code path — expect possible friction there (wrong parameter name, response-shape assumption, etc.), not in the Anthropic path or the settings/usage plumbing around it.

The Anthropic path itself still can't complete a real extraction — `backend/config.json`'s `anthropic_api_key` is a placeholder (`sk-ant-...`), confirmed still true as of this session (this has been an open item across multiple sessions now; the user needs to paste a real key into the LLM Settings modal — gear icon — themselves, since it's a secret this agent shouldn't handle).

## Local environment notes

- Backend running locally on `127.0.0.1:8420` as of end of session (started via `cd backend && ../.venv/Scripts/python.exe -m uvicorn app:app --host 127.0.0.1 --port 8420`). Check `curl -s http://127.0.0.1:8420/health` before assuming it's still up — this repo has repeatedly hit "Failed to fetch" errors across sessions because the backend isn't a persistent service and gets killed between sessions.
- `openai` and `google-genai` are installed in the venv and in `requirements.txt` (uncommitted along with the rest of item 4).
- No secrets were exposed or logged this session. Every real-looking credential touched (test API keys typed into the Settings modal during Playwright verification) was a fake placeholder value (`sk-test-...`), never a real key.

## Suggested skills for the next session

- **`claude-api`** — mandatory read before touching `backend/llm.py` again (it calls the Anthropic SDK directly), per this environment's own trigger rules. Also the source to re-verify model IDs/pricing before updating the `PROVIDERS` table — don't trust the pricing cached in this handoff doc or in the code comments without re-checking, since Sonnet 5's intro pricing is time-boxed (expires 2026-08-31) and Anthropic pricing can change.
- **`code-review`** — the multi-provider dispatch code and usage-tracking cost math (`backend/llm.py`, `backend/db.py`) has only been self-tested this session, never independently reviewed. Worth a pass before it's trusted with real spend tracking across three providers.
- **`security-review`** — `/settings/llm` is new attack surface that stores and dispatches API keys to three different external providers from a local-only backend; worth a second look at the key-masking logic and whether `config.json`'s plaintext-on-disk storage (already the existing pattern for the legacy Anthropic key, not a new risk introduced this session, but now covers three providers instead of one) is still acceptable before this goes further.
- **`run`** — if verifying any further UI changes in a browser; this session used the ad hoc Playwright pattern (`chromium.launch()` against `file://` + the local backend) since no dedicated `run` skill exists for this project yet — worth generating one via `/run-skill-generator` if this pattern keeps getting reinvented session to session.
