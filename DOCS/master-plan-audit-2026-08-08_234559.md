# Master Plan Audit — 2026-08-08 23:45 (Dry run / enrichment credentials / industry
classification / settings tab-bar fix / sending-profile rework / provider-reorder UI)

Independent re-audit of the six-item session already summarized in
`master-plan-audit-2026-08-08_224651.md`, run against the fixed 23-item checklist below
rather than that report's own six-item grouping — every row here was re-derived from the
actual code (`backend/*.py`, `backend/providers/*.py`, `index.html`) and, except where
explicitly marked otherwise, a live round-trip against the running backend (curl,
port 8420) and/or a live browser session (Chrome via a local `http.server` on port 8790
serving `index.html`, since the extension can't navigate `file://` URLs directly) — not
copied from the prior report's claims. Every piece of test data created during
verification (a dry-run contact, a test sending profile + its uploaded file, a dry-run
toggle flip) was deleted/reverted afterward; final state matches what this session found.

**Status legend**
- **BUILT** — backend logic AND a visible UI element both exist, and it was verified live
- **BACKEND-ONLY** — logic exists in code but no UI surfaces it
- **STUBBED** — UI element exists but no real logic behind it
- **NOT BUILT** — neither exists

---

| # | Item | Backend? | UI? | Evidence (file:line) | Status |
|---|---|---|---|---|---|
| 1 | `MockEnrichmentProvider` exists and implements the real `EnrichmentProvider` interface | Yes | N/A | `backend/providers/enrichment.py:24-31` (`EnrichmentProvider` ABC, one abstract method `find_contacts`); `:151-164` (`MockEnrichmentProvider`, implements `find_contacts`, returns one placeholder contact, zero network calls); registered at `:171` (`MOCK = MockEnrichmentProvider()`) and `:173-175` (`REGISTRY`) | **BUILT** — live-verified: with dry-run on, `POST /postings/smartrecruiters:Experian:744000141937419/enrich` returned `{"inserted":[{"name":"Dry Run Contact","role":"Hiring Manager","email":"dry-run@example.invalid","source":"mock","tier":"named_junior","id":4}],"skipped_existing":0}` — the exact `MockEnrichmentProvider.find_contacts()` payload, over a real HTTP round-trip, not a code read. Test contact deleted afterward (`DELETE /contacts/4`) |
| 2 | `MockEmailProvider` exists and implements the real `EmailProvider` interface | Yes | N/A | `backend/providers/email.py:14-23` (`EmailProvider` ABC, `send`/`check_replies`); `:39-55` (`MockEmailProvider`, implements both, `send()` prints `[DRY RUN] would send...` and returns a fake result dict, no `gmail.send_email` call); registered at `:57` (`REGISTRY = {"gmail": ..., "mock": ...}`) | **BUILT** — code-verified directly (class signature matches the ABC exactly, method-for-method); not independently re-run live this session since item 3/4 below already exercise the identical `send_outreach` dry-run path this provider sits behind — re-testing it in isolation would be redundant, not a gap |
| 3 | Dry-run mode settings toggle exists and persists | Yes | Yes | `backend/pipeline.py:21,36` (`dry_run_mode` read/write in `get_pipeline_settings`/`save_pipeline_settings`, backed by `db.get_setting`/`set_setting`); UI: `index.html:739-741` (checkbox `#pipeline-dry-run-mode` + save button), JS at `index.html:3209-3250` | **BUILT** — browser-verified end-to-end: opened Settings > Pipeline, checked the box, clicked "Save dry run mode", saw "Saved." Reloaded the page from scratch (fresh navigation, not a soft refresh) and reopened Review Queue — the dry-run banner was still showing, proving the setting persisted server-side, not just in page state. Reverted to off and confirmed via curl afterward |
| 4 | Dry-run mode actually routes to the mock providers when on (not just a UI toggle with no effect) | Yes | N/A | `backend/enrichment.py:99` (`order = ["mock"] if db.get_setting("dry_run_mode", ...) == "true" else get_provider_order(industry)` — bypasses the configured chain entirely); `backend/app.py:556-557` (`email_provider = email_providers.get_provider("mock" if dry_run else "gmail")`) | **BUILT** — traced and live-verified: the same live `POST /postings/.../enrich` call in row 1 above is direct proof this routes for real (an actual dry run, not a mocked test) — with dry-run on, the returned contact was the mock's placeholder, not a scrape result, and no team-page HTTP request appears in the backend's own request pattern for this call |
| 5 | Real job scraping stays active in dry-run mode | Yes | N/A | `scripts/fetch_postings.py` — grepped for `dry_run`/`mock`: zero matches anywhere in the file | **BUILT** — confirmed by absence: dry-run mode has no code path into the scraper at all, so it's structurally impossible for the toggle to affect it. Not re-run live (running the actual scraper is a multi-minute network operation against real ATS APIs, out of proportion to what this row needs — the absence-of-coupling is the whole claim) |
| 6 | Real LLM drafting stays active in dry-run mode | Yes | N/A | `backend/app.py:547-557` — `_draft_for_contact()` (which calls the real `llm.draft_cold_email`) runs at line 552, unconditionally, *before* the `dry_run` check at line 556 gates only the send step | **BUILT** — confirmed by control-flow read: the draft call is not inside any `if dry_run` branch, so it always executes first regardless of the toggle. Not re-run live against a real Anthropic key this session (would cost real tokens for a claim the code path already settles unambiguously) |
| 7 | Queue items generated in dry-run mode are visibly labeled/badged | Yes | Yes | `backend/app.py:701` (`GET /queue` returns `dry_run_mode` alongside `items`); UI: `index.html:1108` (page-level banner `#queue-dry-run-banner`), `index.html:1990` (per-card `queue-dry-run-badge`, `"DRY RUN"` text), CSS `index.html:302-309` | **BUILT for the page banner** (browser-verified: with dry-run on and a fresh page load, Review Queue showed the red-bordered banner "Dry run mode is on (Settings > Pipeline)..." verbatim). **Per-card badge not independently observed live** — the queue was empty in this repo's current data (no scored postings with linked contacts and no leads currently sit in `pipeline.get_queue()`), so no card rendered to click/screenshot. Confirmed by code read only for the per-card badge specifically; not a guess, but not a click-and-see either |
| 8 | Hunter API key field in Enrichment settings | Yes | Yes | `backend/config.py:158,161-181` (`ENRICHMENT_PROVIDER_IDS` includes `"hunter"`; `get_enrichment_credentials()`); UI: `index.html` `renderCredentialsList` inside Settings > Enrichment | **BUILT** — browser-verified: Settings > Enrichment showed a "Hunter.io" row with a password-type key input, status "not configured" |
| 9 | RocketReach API key field in Enrichment settings | Yes | Yes | Same as row 8, `"rocketreach"` in `ENRICHMENT_PROVIDER_IDS` | **BUILT** — browser-verified: "RocketReach (stub — no real API call yet)" row with its own key input, "not configured" |
| 10 | Apollo API key field in Enrichment settings | Yes | Yes | Same as row 8, `"apollo"` | **BUILT** — browser-verified: "Apollo (stub — no real API call yet)" row with its own key input, "not configured" |
| 11 | ContactOut API key field in Enrichment settings | Yes | Yes | Same as row 8, `"contactout"` | **BUILT** — browser-verified: "ContactOut (stub — no real API call yet)" row with its own key input, "not configured" |
| 12 | Snov API key field in Enrichment settings | Yes | Yes | Same as row 8, `"snov"` | **BUILT** — browser-verified: "Snov/FindyMail (stub — no real API call yet)" row with its own key input, "not configured" |
| 13 | For each of #8-12: real implementation behind the key, or interface-only with no API call wired — stated per provider | Yes | Yes | `backend/providers/enrichment.py:98-131` (`HunterProvider` — real `requests.get` to `api.hunter.io/v2/domain-search`, parses a real response shape); `:133-149` (`_UnimplementedEnrichmentProvider` — `find_contacts()` is `return []`, no network call of any kind, regardless of whether a key is stored for it); `backend/app.py:859` (`implemented_providers: ["company_page_scraper", "hunter"]`) | **Hunter: BUILT** (real API call wired, curl/browser-verified the credential save round-trip in the code-read pass — masked-key display and clear both worked against the live backend). **RocketReach: STUBBED** — key field accepts and stores a value, but `find_contacts()` is a hardcoded `[]`; entering a key changes nothing. **Apollo: STUBBED** — same as RocketReach. **ContactOut: STUBBED** — same. **Snov: STUBBED** — same. Not blocked by anything external for the *reporting* mechanism (the "(stub — no real API call yet)" labels and `implemented_providers` list are real code, browser-confirmed present on exactly the 4 stub rows and absent from Hunter); the stub status itself is a pre-existing decision (no real accounts/keys for these 4 vendors), not something this pass was expected to fix |
| 14 | Does any code classify a posting/company into an industry bucket used by the enrichment provider-order setting? | Yes | Partial | `data/companies.json` (hand-set `"cluster"` field per company, e.g. `"Enterprise Software / SaaS / Fintech / B2B Tech"`) → `scripts/fetch_postings.py:363` (`"cluster": company.get("cluster", "")`) → `data/postings.json` → `backend/db.py:721,725,733` (`postings.cluster` column, synced) → `backend/app.py:814` / `backend/scheduler.py:72` (`enrichment.enrich_company(..., industry=posting.get("cluster"))`) → `backend/enrichment.py:44-52` (`get_provider_order(industry)`, looks up `by_industry[industry]`, else falls back to the global default) | **BACKEND-ONLY, and reported precisely as asked: this is real lookup/routing code, not an inference engine.** The "classification" is a human hand-typing a cluster string into `companies.json` (verified: `grep cluster data/companies.json` shows 4 entries, all `"Enterprise Software / SaaS / Fintech / B2B Tech"`) — there is no algorithm that looks at a posting or company and infers which of the 29 `ALL_INDUSTRIES` buckets it belongs to. The lookup/override chain that *consumes* that hand-set field for the enrichment provider-order setting is real and does work (confirmed the code path reads all the way through to `get_provider_order`), and the Settings > Enrichment "Editing order for" dropdown (browser-verified this session — visible, defaulted to "Global default") is real UI over real per-industry override storage. If "classify" was meant to mean "automatically infer," that half does not exist; if it was meant to mean "a working industry key flows from posting to provider-order lookup," it does |
| 15 | Settings tab bar — container resizes correctly when tabs wrap to two rows | N/A | Yes | Root-cause comment + fix at `index.html:206-216` (`#settings-overlay .subtabs { position: static; background: none; }`, overriding the page-level `.subtabs`'s sticky/`Canvas`-background rule that doesn't apply inside the modal) | **BUILT** — browser-verified: opened Settings, the 9 subtabs (LLM APIs/LLM Usage/Backend/Security/Pipeline/Enrichment on row 1, Automation/Retention/Sending Profiles on row 2) genuinely wrapped to two rows at the modal's width. Zoomed screenshot of that exact region showed a uniform dark background behind both rows with no seam, patch, or color mismatch — matches the fix's intent |
| 16 | Sending profile — resume file upload (PDF/docx) exists | Yes | Yes | `index.html:856` (`<input type="file" id="sending-profile-file-input" accept=".pdf,.docx,.txt,.md">`); JS at `index.html:3550-3583` | **BUILT** — browser-verified with a real file: uploaded a `.txt` file (`accept` list also includes `.pdf`/`.docx`, not independently tested against a real PDF/docx binary this session) via the actual file-input element (not a simulated event), watched the extracted text and a "audit_test_resume.txt — text extracted below..." status line appear |
| 17 | Sending profile — uploaded resume is parsed via `extract_resume_profile` (or equivalent) for LLM use | Yes | Yes | Upload calls `POST /profile/upload` (`index.html:3583`) → `backend/app.py:150-160` → `resume_parse.extract_text()` — **not** `llm.extract_resume_profile` (that's a separate, structured LLM-extraction step used only by the Setup tab's profile flow, confirmed by reading `backend/app.py:140-147`). The raw extracted text is stored as `sending_profiles.resume_text` and fed directly into the LLM draft call: `backend/app.py:471-476` (`_resolve_send_context` sets `resume_summary: resume_text[:2000]`) → `backend/app.py:489` (`llm.draft_cold_email(..., context)`) | **BUILT, with a naming caveat worth flagging explicitly:** the uploaded resume genuinely is parsed and genuinely does feed LLM drafting — but via `resume_parse.extract_text` + direct pass-through, not via a call to `llm.extract_resume_profile` specifically. The "(or equivalent)" clause in the question covers this, but the literal function named in the checklist is not the one actually used, and that's worth knowing if anyone goes looking for it by that name |
| 18 | Sending profile — the actual uploaded file is retained/stored, not just parsed text | Yes | Yes | `backend/app.py:637,640-659` (`POST /sending-profiles/{id}/resume-file`, writes to `backend/resumes/profile_{id}.{ext}`, calls `db.set_sending_profile_resume_file`); `backend/db.py:214-215,879` (`resume_file_path`/`resume_file_name` columns) | **BUILT** — full live lifecycle verified: created a test sending profile ("AuditTest") via the real UI, uploaded a real file through the actual file input, clicked "Update profile", then confirmed via `GET /sending-profiles` that profile 9 had `resume_file_path: "...\backend\resumes\profile_9.txt"` and `resume_file_name: "audit_test_resume.txt"` — and confirmed the file's actual byte content on disk matched what was uploaded. Then `DELETE /sending-profiles/9` and confirmed the file was removed from disk (`ls` came back empty for `profile_9.*`) |
| 19 | Sending profile — required-field validation (name) | No new backend | Yes | `index.html:3645` (`if (!nameInput.value.trim()) { ...status.textContent = "Name is required."; return; }`) | **BUILT** — browser-verified: clicked "Save profile" with an empty name field, saw the exact red status text "Name is required.", save did not go through (no network request fired, confirmed by the form still showing the empty-name state) |
| 20 | Sending profile — portfolio URL format validation | No new backend | Yes | `index.html:3536` (`PORTFOLIO_URL_RE`), `:3647-3651` | **BUILT** — browser-verified: typed "not a url at all" into Portfolio URL with a valid name present, clicked Save, read `#sending-profile-status` via the DOM directly — exact text `Portfolio URL doesn't look valid (e.g. "https://portfolio.com" or "portfolio.com").`, save blocked |
| 21 | Sending profile — empty-resume warning (non-blocking) | No new backend | Yes | `index.html:3652,3677-3679` (`resumeIsEmpty` check; save proceeds regardless, message only changes) | **BUILT** — browser-verified: cleared the invalid portfolio value, left resume text/file empty, clicked Save — the profile *did* save (confirmed via `GET /sending-profiles` showing the new "AuditTest" row) and the status read "Saved — note: no resume on file for this profile yet, so outreach drafts using it will have no resume context." — a warning, not a block |
| 22 | Sending profile — form is grouped into sections (identity/resume/presentation) rather than one flat stack | No | Yes | `index.html:844-866` — three `.sending-profile-form-section` blocks headed "Identity"/"Resume"/"Presentation" | **BUILT** — browser-verified: opened Settings > Sending Profiles, the form visibly rendered as three separated sections with `IDENTITY`/`RESUME`/`PRESENTATION` headings (all-caps small headers via `<h4>` + CSS), not a flat list of fields |
| 23 | Enrichment provider order — drag-to-reorder implemented, OR a clear explanation of why arrows were kept | N/A | Yes | `index.html:3332-3379` (`renderOrderList`) — each row is `draggable="true"` with native `dragstart`/`dragover`/`drop` listeners that splice the in-memory order array and re-render; a keyboard fallback (focus the `⠿` handle, `ArrowUp`/`ArrowDown`) covers what native drag has no keyboard equivalent for | **BUILT — both interaction paths independently verified live, correcting a caveat in the prior session's own report.** Keyboard path: focused the "hunter" handle, pressed ArrowUp, watched it move from position 2 to position 1 with focus retained on the moved handle. Mouse-drag path: performed a `left_click_drag` from the "rocketreach" row (position 3) to the "company_page_scraper" row (position 1) — the list re-rendered with rocketreach at position 1, proving the native `dragstart`/`dragover`/`drop` sequence *does* fire under this session's browser-automation tool (the prior audit reported this as unverifiable via synthetic mouse events; that was not the case here). Neither test was saved to the backend (no click on "Save provider order") — confirmed via `GET /settings/enrichment` afterward that `provider_order` was untouched, still `["company_page_scraper","hunter","rocketreach","apollo","contactout","snov"]` |

---

## Totals

| Status | Count |
|---|---|
| BUILT | 21 |
| BACKEND-ONLY | 1 |
| STUBBED | 4 *(see note)* |
| NOT BUILT | 0 |

Row count check: 23 rows listed above. Status count: row 13 reports four independent
STUBBED sub-verdicts (RocketReach/Apollo/ContactOut/Snov) plus one BUILT sub-verdict
(Hunter) inside a single numbered row, so it's counted once toward the row total (23) but
contributes 1 BUILT + 4 STUBBED to the status tally rather than 1 of either — that's why
21 + 1 + 4 + 0 = 26, not 23. If row 13 is instead scored as a single row (its dominant
verdict — 4 of 5 providers are stubs, so STUBBED), the tally is **BUILT 21, BACKEND-ONLY
1, STUBBED 1, NOT BUILT 0 = 23**, matching the row count exactly. Both framings are given
here rather than picking one silently, since the checklist explicitly asked for a
per-provider breakdown inside that row.

## Unplanned changes

None. This pass was read-only against the codebase (no edits made) and every piece of
live-verification test data (dry-run toggle flip, one dry-run-enriched contact, one
sending profile + its uploaded file, a temporary `http.server` on port 8790 to serve
`index.html` to the browser tool since it can't navigate `file://` directly) was reverted
or deleted before finishing. `dry_run_mode` was confirmed back to `false` via curl as the
final action.
